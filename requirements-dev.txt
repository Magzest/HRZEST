# Install alongside requirements.txt for local development and testing:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.1.1
pytest-cov>=5.0.0
pytest-mock>=3.14.0
# Randomizes test order each run (seeded + printed, so a failure is
# reproducible with `pytest -p randomly --randomly-seed=<seed>`) -- added
# alongside tests/conftest.py's per-test DB snapshot/restore specifically so
# order-dependence can't silently creep back in unnoticed.
pytest-randomly>=3.15.0

# ── Code quality ──────────────────────────────────────────────────────────────
flake8>=7.3.0

# ── Static asset minification (see minify_static.py) ───────────────────────────
rcssmin>=1.2.2
rjsmin>=1.2.5

# ── End-to-end browser tests (see tests_e2e/README.md) ──────────────────────────
# Separate from the main pytest suite (pytest.ini's testpaths = tests excludes
# this directory already) -- these drive a real browser against a real
# running instance of the app, not the Flask test client, so they need their
# own browser binary (`playwright install chromium`) and are meaningfully
# slower. Run with: pytest tests_e2e/ --browser chromium
pytest-playwright>=0.5.2
